[project]
name = "metrobus-rl-env"
version = "0.1.0"
description = "Istanbul Metrobus RL Environment — Gymnasium compatible"
requires-python = ">=3.10"
dependencies = [
    "gymnasium>=0.29",
    "numpy>=1.24",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
